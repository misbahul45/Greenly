# Greenly Flutter app environment.
# This app uses flutter_dotenv and loads .env as a Flutter asset.
# Copy this file to apps/app/.env before running.
#
# Physical Android device: use http://<your-machine-lan-ip>/api.
# Do not use localhost for testing on a physical Android phone.

API_URL=http://YOUR_MAC_IP/api
